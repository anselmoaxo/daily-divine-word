# Variáveis privadas do servidor Nitro. Nunca use o prefixo VITE_ para segredos.
NITRO_SUPABASE_URL=https://seu-projeto.supabase.co
NITRO_SUPABASE_SERVICE_ROLE_KEY=
# A autorização do cancelamento usa um JWT do Supabase Auth. Conceda o papel
# `cancellation_admin` ou `admin` exclusivamente em app_metadata do usuário.
